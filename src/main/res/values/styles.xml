<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/gray</item>
        <item name="colorPrimaryDark">@color/material_blue_grey_800</item>
        <item name="colorAccent">@color/primary_dark_material_light</item>
    </style>

    <style name="Base.Widget.Design.TabLayout" parent="android:Widget">
        <!--item name="tabBackground">@drawable/tab_selected_color</item-->
        <item name="tabTextColor">@color/font_brown_dark</item>
        <!--item name="tabSelectedTextColor">@color/violet_statusbar</item-->
        <item name="tabMode">scrollable</item>
        <item name="tabGravity">fill</item>
        <item name="tabIndicatorColor">@color/white</item>
        <item name="tabIndicatorHeight">4dp</item><!--Indicater bar in tabs-->
        <item name="tabPaddingStart">20dp</item>
        <item name="tabPaddingEnd">20dp</item>
    </style>

    <style name="NavigationTheme" parent="AppTheme">
        <item name="android:textSize">16sp</item>
        <item  name="android:textStyle">bold</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="MyTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/MyTheme.ActionBarStyle</item>
    </style>

    <style name="MyTheme.ActionBarStyle" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:titleTextStyle">@style/MyTheme.ActionBar.TitleTextStyle</item>
    </style>

    <style name="MyTheme.ActionBar.TitleTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
